' Deterministic Material dns icon stub.
!include <material/common>

!procedure MA_DNS($a, $b=dns, $c=, $d=, $e=__PUML_MISSING__)
!if $e != __PUML_MISSING__
MaterialIcon($c, dns, %unquote($e), "")
!else
MaterialIcon($a, dns, %unquote($b), %unquote($c))
!endif
!endprocedure
