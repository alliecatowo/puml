# Known Limitations

This page has been superseded by the canonical PlantUML support matrix:

- **[`docs/internal/spec/plantuml-spec.md`](../internal/spec/plantuml-spec.md)** — full
  support matrix with per-chapter `✅ / 🟡 / ❌` status.
- **[`docs/internal/spec/audit/`](../internal/spec/audit/)** — 27 per-chapter audits
  with file:line evidence for every documented PlantUML feature.

The new spec is more complete and more conservative than the limitations list
that used to live here, and is the single source of truth for what `puml`
supports relative to PlantUML.
