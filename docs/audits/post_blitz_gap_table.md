# Post-Blitz Parity Gap Table

Status date: 2026-05-17  
Scope: docs/audit/oracle planning only. This table sharpens the remaining work after the local parity blitz through PR `#232` merge commit `04a0d387703d1cbe9a5bfe24889d72ca904b1781`.

## Audited Inputs

| Input | Evidence found | Planning consequence |
|---|---|---|
| Canonical source of truth | `docs/audits/plantuml_parity_source_of_truth.md` marks many rows `implemented`, including PR `#224` progress for `math`, `sdl`, and `ditaa` special-adapter rows; PR `#228` depth evidence for sequence, core UML relations, Gantt, WBS, and Salt; PR `#231` evidence for slanted arrow-head rendering, preprocessor expression depth, nested group frames, MindMap/WBS node color tags, and oracle dry-run categories; and PR `#232` evidence for dotted/hash-padded autonumbering, layout-safe participant ordering, broader core-UML relation/skinparam surfaces, chart axis/legend styling, and preprocessor collection/function breadth. It intentionally keeps advanced breadth rows `partial` where evidence is baseline/depth-limited or not oracle-active. | Treat `partial` rows as the implementation backlog even when their original issue or PR is closed. |
| Core CSV export | `docs/audits/parity_gap_core.csv` still has partial rows for sequence advanced, core-UML advanced semantics, timing advanced, preprocessor advanced/builtins/policy, and data projection even after PR `#232` autonumber, core-UML, chart, and preprocessor improvements. | Next slices should target breadth and fidelity, not family bring-up. |
| Non-UML CSV export | `docs/audits/parity_gap_nonuml.csv` now treats math, SDL, and ditaa as implemented, while still keeping partial rows for gantt/chronology scale/calendar depth, mindmap/WBS styling, Salt advanced styling, Archimate relation/style breadth, nwdiag topology depth, JSON/YAML projection, regex/EBNF advanced semantics, and chart style/legend/axis depth. PR `#228` strengthens Gantt project-end, WBS orientation, and Salt style/header evidence; PR `#231` strengthens MindMap/WBS color-tag evidence; PR `#232` strengthens chart axis/legend styling evidence without completing the full chart surface. | Non-UML work is now mostly depth hardening after parser/render baselines. |
| Audit tests | `tests/parity_csv_audit.rs` enforces machine-readable statuses and official PlantUML references. | Keep future rows conservative and fixture-backed. |
| Oracle tests/workflows | `tests/oracle_smoke.rs`, `.github/workflows/oracle.yml`, `.github/workflows/differential-oracle-smoke.yml`, `scripts/oracle.sh`, and `scripts/differential_oracle_smoke.py` establish skip-sentinel/full-oracle/smoke paths plus PR `#231` dry-run fixture categories. `docs/benchmarks/oracle_report.json` currently contains `{"skipped":true,"reason":"PUML_ORACLE_JAR not set"}`. | The oracle harness is comparison-only and never runtime. Durable conformance evidence still needs active JAR runs and targeted fixture expansion. |
| Examples | `docs/examples/**` includes rendered `.puml`/`.svg` examples for advanced families such as activity, class, archimate, chart, chronology, gantt, mindmap, nwdiag, regex, EBNF, math, SDL, ditaa. | Examples are useful seeds for oracle fixture expansion; they are not by themselves proof of PlantUML parity. |
| GitHub project | `gh project list --owner alliecatowo --format json` shows project `PUML` as user project `#3`, open, 83 items, 22 fields. | Project status is active and broad parity epics remain the best owner for partial-depth follow-up. |

## GitHub Project Snapshot

| Area | Current project state | Evidence | Planning note |
|---|---|---|---|
| Project board | `PUML` project is open with 83 items and 22 fields. | `gh project list --owner alliecatowo --format json` | Use project status as review-flow signal, not as implementation proof. |
| Non-UML blitz issues | `#94`, `#95`, `#96`, `#97`, `#98`, `#99`, `#100`, `#101`, `#102`, `#104`, `#105`, `#106`, `#207`, and `#208` are closed and in `Done`. | `gh issue view <n> --json number,title,state,projectItems,url` | Closed issue means the tracked slice landed; keep CSV partials when the official surface is still wider. |
| Projection review status | `#103` is closed as completed and in project status `Done`. | `gh issue view 103 --json number,title,state,projectItems,url` | Keep JSON/YAML projection in the gap list only for remaining breadth beyond the completed tracked slice. |
| Active parity tracks | `#82`, `#83`, `#84`, `#85`, `#86`, `#88`, `#89`, `#123`, `#124`, `#125`, `#126`, and `#127` are open and `In Progress`; `#87` is closed and should be `Done` on the project. | `gh issue view <n> --json number,title,state,url,projectItems,labels` | The remaining work should attach to the open epics/tracks rather than reopening completed blitz issues. Closed project items should be moved to `Done` only when the issue is already closed. |
| Adjacent open work | `#91`, `#92`, `#184`, `#185`, `#186`, `#190`, `#211`, `#213`, and `#214` are open. | `gh issue list --state open --limit 80 --json number,title,state,url,projectItems,labels` | Useful adjacent work, but lower priority than oracle-backed parity gaps for this audit. |

## Latest Row Classification

| Classification | Rows | Evidence basis | Planning consequence |
|---|---|---|---|
| Now eligible for `implemented` | `math,core+advanced`; `sdl,core+advanced`; `ditaa,core+advanced` | `docs/audits/plantuml_parity_source_of_truth.md`; `docs/audits/parity_gap_nonuml.csv`; PR `#224` merge commit `c68b2a701953c0a018cef1847448334377933cf3`; `gh issue view 105`, `gh issue view 207`, and `gh issue view 208` Done/closed evidence | Keep these out of the remaining gap table unless future oracle runs find regressions or wider incompatible behavior. |
| Still `partial` after latest merged evidence | Sequence advanced, preprocessor advanced, core UML advanced semantics, Gantt/chronology advanced scale/calendar/resources, WBS/mindmap advanced styling, Salt advanced renderer, regex advanced semantics, EBNF advanced style, chart advanced axis/legend/style | PR `#232` / commit `04a0d387703d1cbe9a5bfe24889d72ca904b1781`, PR `#231` / commit `b352788c9a10a9e4bfb96fa52858a66133765acb`, PR `#228` / commit `753858fcfe8bc694e4141cd64ba63382852e4ee9`, and PR `#224` / commit `c68b2a701953c0a018cef1847448334377933cf3` landed stronger deterministic subset evidence, but each row's own notes still name a wider PlantUML surface not covered by the current implementation or not yet backed by active oracle runs. | Track as depth-hardening slices; do not reopen parser-baseline issues just because advanced breadth remains. |
| Still `partial` from prior post-blitz audit | Timing advanced, preprocessor policy, JSON/YAML projection breadth, Archimate advanced, nwdiag advanced | Canonical source-of-truth and CSV exports keep these rows partial despite closed baseline issues. | Keep prioritizing oracle-backed vertical slices over broad issue-status churn. |

## Remaining High-Impact Implementation Slices

| Priority | Slice | What remains | Exact evidence | Suggested next implementation slice |
|---|---|---|---|---|
| P0 | Oracle conformance activation | Oracle comparison infrastructure exists, but latest checked-in report is a skip sentinel because `PUML_ORACLE_JAR` was not set. The oracle is never linked into or required by runtime rendering. | `docs/benchmarks/oracle_report.json`; `scripts/oracle.sh`; `docs/oracle-thresholds.md`; `.github/workflows/oracle.yml`; `tests/oracle_smoke.rs` | Run an active JAR-backed oracle pass, triage top `drift`/`jar-only`/`puml-only` categories, then promote the highest-value fixtures into deterministic smoke coverage. |
| P0 | Sequence advanced fidelity | Participant order controls, decorated-arrow coverage, distinct slanted half-head rendering, dotted portability arrows, same-row `&` parallel message layout, and dotted/hash-padded autonumber depth are landed, but broader arrow/style breadth and full teoz collision routing remain partial. | `docs/audits/parity_gap_core.csv` rows `sequence,advanced,Advanced arrow/style breadth,partial` and `sequence,advanced,Teoz-specific semantics,partial`; `tests/fixtures/arrows/valid_expanded_forms.puml`; `tests/fixtures/arrows/valid_dotted_parallel_sequence_edges.puml`; `tests/render_e2e.rs::render_sequence_participant_order_controls_lifeline_placement`; `tests/render_e2e.rs::render_sequence_slanted_arrow_heads_are_distinct`; `tests/render_e2e.rs::render_sequence_dotted_parallel_edges_share_teoz_row_deterministically`; `tests/fixtures/structure/valid_autonumber_dotted_and_hash_padding.puml`; `tests/fixtures/notes/valid_note_across_multi.puml`; PR `#228`; PR `#231`; PR `#232` | Implement one oracle-backed sequence batch around remaining uncommon arrows, inline styling, note/ref placement, autonumber edge formatting, and teoz routing divergence. |
| P0 | Core UML semantic styling depth | Class/object/usecase/component/deployment/state/activity advanced rows remain partial even after PR `#232` broadened relation and skinparam surfaces; current fixtures prove support, not full PlantUML breadth. | `docs/audits/parity_gap_core.csv` advanced rows for `class`, `object`, `usecase`, `component`, `deployment`, `state`, `activity`; fixtures under `tests/fixtures/families/` and `tests/fixtures/styling/`; examples under `docs/examples/class/`, `docs/examples/component/`, `docs/examples/deployment/`, `docs/examples/activity_new/`; PR `#232` | Pick one family cluster, preferably class/component/deployment styling, and close a vertical slice from parser flags through SVG oracle comparison. |
| P0 | Gantt/chronology scale and calendar depth | Project start/end markers and current render path are active, but advanced scale/calendar/resource semantics remain narrower than PlantUML. | `docs/audits/parity_gap_nonuml.csv` row `gantt+chronology,layout+renderer,time-axis and event render,partial`; `tests/fixtures/timeline/valid_gantt_render.puml`; `tests/fixtures/timeline/valid_gantt_dates_proportional.puml`; `tests/fixtures/timeline/valid_chronology_render.puml`; `tests/parity_wave_csv_timeline_activity.rs`; PR `#228` | Add calendar/scale/resource fixtures that force nontrivial date axes, exclusions, dependencies, milestones, and resource lanes, then align renderer metrics against the Java oracle. |
| P1 | Preprocessor expression and macro depth | Core preprocessor plus function-like `!define`, eval helpers, nested list/map expression depth, and PR `#232` collection/function breadth are implemented, but dynamic invocation edge cases, broader expression breadth, deterministic builtin breadth, macro concatenation, and URL policy remain partial. | `docs/audits/parity_gap_core.csv` rows for `preprocessor,advanced` and `preprocessor,policy`; `tests/fixtures/preprocessor/valid_dynamic_call_user_func.puml`; `tests/fixtures/preprocessor/valid_macro_expr_collection_depth.puml`; `tests/fixtures/preprocessor/valid_builtin_color_math.puml`; `tests/fixtures/errors/invalid_include_url.puml`; PR `#231`; PR `#232` | Implement one constrained expression/macro compatibility wave while preserving deterministic rejection for remote/unsafe behavior. |
| P1 | JSON/YAML projection into UML | Standalone data diagrams and the tracked projection slice are complete, but broader cross-diagram projection behavior remains partial. | `docs/audits/parity_gap_core.csv` row `data,projection,Cross-diagram JSON/YAML projection adapters,partial`; `docs/audits/parity_gap_nonuml.csv` row `json+yaml,cross-diagram projection,JSON/YAML projection into UML contexts,partial`; `tests/fixtures/families/valid_yaml_projection.puml`; GitHub issue `#103` closed with project status `Done` | Review projection behavior against object/usecase/component contexts and add oracle fixtures for unsupported or divergent projection forms. |
| P1 | Salt widget/style breadth | Salt parser and renderer now cover label/input/button/combo/checkbox/radio rows including compact `[]`/`()` forms, prefix-label rows, separators, tree/menu/tab/scroll/table widgets, text-area and scroll-pane containers, advanced table empty/span/header markers, group boxes, and deterministic style/header handling, but full Creole/icon/style semantics remain partial. | `docs/audits/parity_gap_nonuml.csv` row `salt,renderer,wireframe widget rendering,partial`; `tests/fixtures/families/valid_salt_login_form.puml`; `tests/integration.rs`; PR `#224`; PR `#228`; local Salt lane test `salt_compact_controls_textarea_advanced_table_and_style_blocks_render` | Add oracle checks for Creole/OpenIconic/pseudo-sprite controls, nested group boxes, and richer theme/style inheritance. |
| P1 | Archimate and nwdiag layout semantics | Both families render deterministically, but richer relation/style/topology semantics remain partial. | `docs/audits/parity_gap_nonuml.csv` rows for `archimate,relation layout+render` and `nwdiag/network,layout+render`; `docs/examples/archimate/01_layered.svg`; `docs/examples/nwdiag/01_single_net.svg`; `tests/integration.rs` | Split into two compact batches: Archimate relation macros/junction visual semantics, then nwdiag groups/multiple networks/addressing. |
| P2 | Regex and EBNF advanced rendering | Baseline renderers now include localized regex labels, exact/ranged counted quantifier evidence, styled EBNF rule notes, and exact/ranged counted repeat evidence, but broader Unicode/category regex semantics and full railroad styling remain partial. | `docs/audits/parity_gap_nonuml.csv` rows `regex,rendering+localized labels,advanced regex descriptive options,partial` and `ebnf,rendering+style,full railroad/style semantics,partial`; `docs/examples/regex/01_character_classes.svg`; `docs/examples/ebnf/01_simple_grammar.svg`; `tests/integration.rs`; PR `#224` / commit `c68b2a701953c0a018cef1847448334377933cf3` | Expand oracle fixtures for Unicode/category classes, grouping, alternation, localized labels, optional/repetition variants, and railroad style controls. |
| P2 | Chart axis/legend/style integration | Chart parser supports multiple baseline forms plus PlantUML-style chart labels, colon-delimited points, per-slice pie colors/labels/legend items, normalized palette/caption/annotations, explicit v-axis tick step, legend off/positioning/style aliases, per-axis color/text/grid suffixes, semantic tick/grid SVG classes, and selected skinparams, but full axis/legend semantics remain partial. | `docs/audits/parity_gap_nonuml.csv` row `chart,chart renderer+style,axis/legend/style breadth,partial`; `docs/examples/chart/01_bar.svg`; chart examples under `docs/examples/chart/`; `tests/integration.rs`; `tests/chart_parity.rs`; PR `#224` / commit `c68b2a701953c0a018cef1847448334377933cf3`; PR `#232` / commit `04a0d387703d1cbe9a5bfe24889d72ca904b1781`; local chart/MindMap/WBS parity chunk on 2026-05-17 | Continue with v2-axis/combined label-positioning, data-label fixture expansion, and oracle fixtures. |
| P2 | Mindmap/WBS orientation and styling | Parser/model/render support now carries per-node `[#color]` tags for MindMap/WBS, MindMap orientation metadata and left/right side classes, WBS horizontal orientations with side-aware edge anchors, and WBS checkbox/progress annotation style classes, but full large-tree/style parity remains partial. | `docs/audits/parity_gap_nonuml.csv` row `mindmap+wbs,layout+renderer,hierarchy layout/orientation,partial`; `docs/examples/mindmap/01_basic.svg`; `docs/examples/wbs/01_basic.svg`; `tests/parity_wave_csv_timeline_activity.rs`; `tests/nonuml_topology_depth.rs`; PR `#228`; PR `#231`; non-UML breadth wave on 2026-05-16; local chart/MindMap/WBS parity chunk on 2026-05-17 | Add larger left/right fixtures and deeper style controls before tuning layout against the Java oracle. |

## Recommended Execution Order

1. Activate the oracle and collect durable conformance data before expanding more breadth.
2. Use oracle output to pick the first P0 vertical slice: sequence advanced fidelity, core UML styling depth, or gantt/chronology date/resource depth.
3. Keep `#103` closed/Done, but audit JSON/YAML projection breadth with oracle fixtures before promoting the CSV projection row.
4. Batch P1/P2 non-UML renderer-depth work only after the P0 slices have oracle-backed drift categories.

## Status Guardrails

- Do not convert `partial` rows to `implemented` from local render evidence alone.
- Do not infer parity from closed blitz issues when CSV/source-of-truth rows still say `partial`.
- Prefer fixture-backed, oracle-observable deltas over broad tracking issue updates.
- Treat oracle output as comparison evidence only; do not describe the Java oracle as a runtime dependency or fallback renderer.
- Keep `docs/audits/plantuml_parity_source_of_truth.md` canonical; this file is the sharper post-blitz planning companion.
