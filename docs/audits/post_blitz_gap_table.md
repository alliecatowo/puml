# Post-Blitz Parity Gap Table

Status date: 2026-05-17  
Scope: docs/audit/oracle planning only. This table sharpens the remaining work after the local parity blitz on `codex/local-parity-blitz-20260516`.

## Audited Inputs

| Input | Evidence found | Planning consequence |
|---|---|---|
| Canonical source of truth | `docs/audits/plantuml_parity_source_of_truth.md` marks many rows `implemented`, including now-eligible `math`, `sdl`, and `ditaa` special-adapter rows, but intentionally keeps advanced breadth rows `partial` where evidence is baseline/depth-limited. | Treat `partial` rows as the implementation backlog even when their original issue is closed. |
| Core CSV export | `docs/audits/parity_gap_core.csv` still has partial rows for sequence advanced, core-UML advanced semantics, timing advanced, preprocessor advanced/builtins/policy, and data projection. | Next slices should target breadth and fidelity, not family bring-up. |
| Non-UML CSV export | `docs/audits/parity_gap_nonuml.csv` now treats math, SDL, and ditaa as implemented, while still keeping partial rows for gantt/chronology render depth, mindmap/WBS styling, Salt advanced styling, Archimate relation/style breadth, nwdiag topology depth, JSON/YAML projection, regex/EBNF advanced semantics, and chart style/legend/axis depth. | Non-UML work is now mostly depth hardening after parser/render baselines. |
| Audit tests | `tests/parity_csv_audit.rs` enforces machine-readable statuses and official PlantUML references. | Keep future rows conservative and fixture-backed. |
| Oracle tests/workflows | `tests/oracle_smoke.rs`, `.github/workflows/oracle.yml`, `.github/workflows/differential-oracle-smoke.yml`, `scripts/oracle.sh`, and `scripts/differential_oracle_smoke.py` establish skip-sentinel/full-oracle/smoke paths. `docs/benchmarks/oracle_report.json` currently contains `{"skipped":true,"reason":"PUML_ORACLE_JAR not set"}`. | The oracle harness exists, but durable conformance evidence still needs active JAR runs and targeted fixture expansion. |
| Examples | `docs/examples/**` includes rendered `.puml`/`.svg` examples for advanced families such as activity, class, archimate, chart, chronology, gantt, mindmap, nwdiag, regex, EBNF, math, SDL, ditaa. | Examples are useful seeds for oracle fixture expansion; they are not by themselves proof of PlantUML parity. |
| GitHub project | `gh project list --owner alliecatowo --format json` shows project `PUML` as user project `#3`, open, 83 items, 22 fields. | Project status is active and broad parity epics remain the best owner for partial-depth follow-up. |

## GitHub Project Snapshot

| Area | Current project state | Evidence | Planning note |
|---|---|---|---|
| Project board | `PUML` project is open with 83 items and 22 fields. | `gh project list --owner alliecatowo --format json` | Use project status as review-flow signal, not as implementation proof. |
| Non-UML blitz issues | `#94`, `#95`, `#96`, `#97`, `#98`, `#99`, `#100`, `#101`, `#102`, `#104`, `#105`, `#106`, `#207`, and `#208` are closed and in `Done`. | `gh issue view <n> --json number,title,state,projectItems,url` | Closed issue means the tracked slice landed; keep CSV partials when the official surface is still wider. |
| Projection review exception | `#103` is closed but still in project status `Human Review`. | `gh issue view 103 --json number,title,state,projectItems,url` | JSON/YAML projection should remain a high-signal review/follow-up slice before being treated as done on the board. |
| Active parity tracks | `#82`, `#83`, `#84`, `#85`, `#86`, `#87`, `#88`, `#89`, `#123`, `#124`, `#125`, `#126`, and `#127` are open and `In Progress`. | `gh issue list --state open --limit 80 --json number,title,state,url,projectItems,labels` | The remaining work should attach to these epics/tracks rather than reopening completed blitz issues. |
| Adjacent open work | `#91`, `#92`, `#184`, `#185`, `#186`, `#190`, `#211`, `#213`, and `#214` are open. | `gh issue list --state open --limit 80 --json number,title,state,url,projectItems,labels` | Useful adjacent work, but lower priority than oracle-backed parity gaps for this audit. |

## Latest Row Classification

| Classification | Rows | Evidence basis | Planning consequence |
|---|---|---|---|
| Now eligible for `implemented` | `math,core+advanced`; `sdl,core+advanced`; `ditaa,core+advanced` | `docs/audits/plantuml_parity_source_of_truth.md`; `docs/audits/parity_gap_nonuml.csv`; latest pushed branch `origin/codex/local-parity-blitz-20260516`; `gh issue view 105`, `gh issue view 207`, and `gh issue view 208` Done/closed evidence | Keep these out of the remaining gap table unless future oracle runs find regressions or wider incompatible behavior. |
| Still `partial` after latest pushed evidence | Salt advanced renderer, regex advanced semantics, EBNF advanced style, chart advanced axis/legend/style | Latest pushed commit `13f92c0` adds stronger deterministic subset evidence, but each row's own notes still name a wider PlantUML surface not covered by the current implementation. | Track as depth-hardening slices; do not reopen parser-baseline issues just because advanced breadth remains. |
| Still `partial` from prior post-blitz audit | Sequence advanced, core UML advanced family semantics, timing advanced, gantt/chronology advanced scale/resources, preprocessor advanced/policy, JSON/YAML projection, mindmap/WBS advanced, Archimate advanced, nwdiag advanced | Canonical source-of-truth and CSV exports keep these rows partial despite closed baseline issues. | Keep prioritizing oracle-backed vertical slices over broad issue-status churn. |

## Remaining High-Impact Implementation Slices

| Priority | Slice | What remains | Exact evidence | Suggested next implementation slice |
|---|---|---|---|---|
| P0 | Oracle conformance activation | Full oracle infrastructure exists, but latest checked-in report is a skip sentinel because `PUML_ORACLE_JAR` was not set. | `docs/benchmarks/oracle_report.json`; `scripts/oracle.sh`; `docs/oracle-thresholds.md`; `.github/workflows/oracle.yml`; `tests/oracle_smoke.rs` | Run an active JAR-backed oracle pass, triage top `drift`/`jar-only`/`puml-only` categories, then promote the highest-value fixtures into deterministic smoke coverage. |
| P0 | Sequence advanced fidelity | Arrow/style breadth and teoz-specific semantics remain partial despite deterministic baseline coverage. | `docs/audits/parity_gap_core.csv` rows `sequence,advanced,Advanced arrow/style breadth,partial` and `sequence,advanced,Teoz-specific semantics,partial`; `tests/fixtures/arrows/valid_expanded_forms.puml`; `tests/fixtures/notes/valid_note_across_multi.puml` | Implement one oracle-backed sequence batch around uncommon arrows, inline styling, note/ref placement, and teoz layout divergence. |
| P0 | Core UML semantic styling depth | Class/object/usecase/component/deployment/state/activity advanced rows remain partial; current fixtures prove support, not full PlantUML breadth. | `docs/audits/parity_gap_core.csv` advanced rows for `class`, `object`, `usecase`, `component`, `deployment`, `state`, `activity`; fixtures under `tests/fixtures/families/` and `tests/fixtures/styling/`; examples under `docs/examples/class/`, `docs/examples/component/`, `docs/examples/deployment/`, `docs/examples/activity_new/` | Pick one family cluster, preferably class/component/deployment styling, and close a vertical slice from parser flags through SVG oracle comparison. |
| P0 | Gantt/chronology scale and calendar depth | Render path is active, but advanced scale/calendar/resource semantics remain narrower than PlantUML. | `docs/audits/parity_gap_nonuml.csv` row `gantt+chronology,layout+renderer,time-axis and event render,partial`; `tests/fixtures/timeline/valid_gantt_render.puml`; `tests/fixtures/timeline/valid_gantt_dates_proportional.puml`; `tests/fixtures/timeline/valid_chronology_render.puml`; `tests/parity_wave_csv_timeline_activity.rs` | Add calendar/scale/resource fixtures that force nontrivial date axes, exclusions, dependencies, milestones, and resource lanes, then align renderer metrics against the Java oracle. |
| P1 | Preprocessor expression and macro depth | Core preprocessor is implemented, but dynamic invocation, expression breadth, deterministic builtins, macro concatenation, and URL policy remain partial. | `docs/audits/parity_gap_core.csv` rows for `preprocessor,advanced` and `preprocessor,policy`; `tests/fixtures/preprocessor/valid_dynamic_call_user_func.puml`; `tests/fixtures/preprocessor/valid_builtin_color_math.puml`; `tests/fixtures/errors/invalid_include_url.puml` | Implement one constrained expression/macro compatibility wave while preserving deterministic rejection for remote/unsafe behavior. |
| P1 | JSON/YAML projection into UML | Standalone data diagrams are implemented, but cross-diagram projection remains partial and board status still needs human review. | `docs/audits/parity_gap_core.csv` row `data,projection,Cross-diagram JSON/YAML projection adapters,partial`; `docs/audits/parity_gap_nonuml.csv` row `json+yaml,cross-diagram projection,JSON/YAML projection into UML contexts,partial`; `tests/fixtures/families/valid_yaml_projection.puml`; GitHub issue `#103` closed with project status `Human Review` | Review projection behavior against object/usecase/component contexts, add oracle fixtures, then update project status if review confirms the slice. |
| P1 | Salt widget/style breadth | Salt parser and renderer now cover label/input/button/combo/checkbox/radio rows, prefix-label rows, separators, and tree/menu/tab/scroll/table widgets, but advanced styling semantics remain partial. | `docs/audits/parity_gap_nonuml.csv` row `salt,renderer,wireframe widget rendering,partial`; `tests/fixtures/families/valid_salt_login_form.puml`; `tests/integration.rs`; latest pushed commit `13f92c0` | Add style-focused fixtures and oracle checks for widget theming, layout controls, and nested UI-control combinations. |
| P1 | Archimate and nwdiag layout semantics | Both families render deterministically, but richer relation/style/topology semantics remain partial. | `docs/audits/parity_gap_nonuml.csv` rows for `archimate,relation layout+render` and `nwdiag/network,layout+render`; `docs/examples/archimate/01_layered.svg`; `docs/examples/nwdiag/01_single_net.svg`; `tests/integration.rs` | Split into two compact batches: Archimate relation macros/junction visual semantics, then nwdiag groups/multiple networks/addressing. |
| P2 | Regex and EBNF advanced rendering | Baseline renderers now include localized regex labels, exact/ranged counted quantifier evidence, styled EBNF rule notes, and exact/ranged counted repeat evidence, but broader Unicode/category regex semantics and full railroad styling remain partial. | `docs/audits/parity_gap_nonuml.csv` rows `regex,rendering+localized labels,advanced regex descriptive options,partial` and `ebnf,rendering+style,full railroad/style semantics,partial`; `docs/examples/regex/01_character_classes.svg`; `docs/examples/ebnf/01_simple_grammar.svg`; `tests/integration.rs`; latest pushed commit `13f92c0` | Expand oracle fixtures for Unicode/category classes, grouping, alternation, localized labels, optional/repetition variants, and railroad style controls. |
| P2 | Chart axis/legend/style integration | Chart parser supports multiple baseline forms plus PlantUML-style chart labels, colon-delimited points, annotations, palette, and selected skinparams, but full axis/legend semantics remain partial. | `docs/audits/parity_gap_nonuml.csv` row `chart,chart renderer+style,axis/legend/style breadth,partial`; `docs/examples/chart/01_bar.svg`; chart examples under `docs/examples/chart/`; `tests/integration.rs`; latest pushed commit `13f92c0` | Implement axis controls, legends, multi-series style controls, and oracle fixtures as a single renderer-depth wave. |
| P2 | Mindmap/WBS orientation and styling | Parser and deterministic renderer exist, but orientation/styling parity depth remains partial. | `docs/audits/parity_gap_nonuml.csv` row `mindmap+wbs,layout+renderer,hierarchy layout/orientation,partial`; `docs/examples/mindmap/01_basic.svg`; `docs/examples/wbs/01_basic.svg` | Add left/right orientation, color, depth, and large-tree fixtures before tuning layout. |

## Recommended Execution Order

1. Activate the oracle and collect durable conformance data before expanding more breadth.
2. Use oracle output to pick the first P0 vertical slice: sequence advanced fidelity, core UML styling depth, or gantt/chronology date/resource depth.
3. Resolve `#103` project review status after a focused JSON/YAML projection audit.
4. Batch P1/P2 non-UML renderer-depth work only after the P0 slices have oracle-backed drift categories.

## Status Guardrails

- Do not convert `partial` rows to `implemented` from local render evidence alone.
- Do not infer parity from closed blitz issues when CSV/source-of-truth rows still say `partial`.
- Prefer fixture-backed, oracle-observable deltas over broad tracking issue updates.
- Keep `docs/audits/plantuml_parity_source_of_truth.md` canonical; this file is the sharper post-blitz planning companion.
